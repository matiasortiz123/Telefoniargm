@using Commons.Models;
@model Page<modulo_documentacion.Areas.Admin.Models.Basicas.SituacionLaboral>
<box-header title="Lista de Datos">
    <box-search update-div="TablaSituacionLaboral">
        <div class="input-group input-group-sm pull-right" style="max-width: 300px;">
            <a onclick="nuevaSituacionLaboral()" class="btn btn-info btn-sm">
                <i class="fa fa-plus"><span class="hidden-xs"> Nuevo</span></i>
            </a>
        </div>
    </box-search>
</box-header>
<box-body table="true">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    Descripcion
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items.OrderBy(x => x.Descripcion))
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td>
                        <a onclick="editaSituacionLaboral(@item.Id)" title="Editar" class="btn btn-primary btn-xs"><i class="fa fa-file-text-o"></i></a>
                        <!--<a onclick="Eliminar('@item.Id')" title="Eliminar" class="btn btn-info btn-xs btn-danger"><i class="fa fa-trash"></i></a>-->
                        <a asp-action="Delete"
                           asp-route-id="@item.Id"
                           title="Eliminar"
                           confirm="Este registro ya no podra visualizarse"
                           confirm-icon="Danger"
                           class="btn btn-danger btn-xs">
                            <i class="fa fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</box-body>
<box-footer page-update-div="TablaSituacionLaboral"></box-footer>
<modal title="Edicion de Situacion Laboral" function="editaSituacionLaboral" load-url="/Admin/SituacionLaboral/_Edit/" size="Medium"></modal>
<modal title="Nueva Situacion Laboral" function="nuevaSituacionLaboral" load-url="/Admin/SituacionLaboral/_Create/" size="Medium"></modal>
<!--<modal title="Eliminar Situacion Laboral" function="Eliminar" load-url="/Admin/SituacionLaboral/_Delete/" size="Medium"></modal>-->