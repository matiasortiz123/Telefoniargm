@model TelefoniaCargas.Models.Equipo

@{ ViewData["Title"] = "Crear"; }


<div class="row">
    <div class="col-sm-12 p-3">
        <form method="post" asp-action="_Asignar">
            <div class="form-group">
                <label asp-for="Persona_Dependencia.Dependencia.Unidad" class="control-label"></label>
                <select asp-for="Persona_Dependencia.Dependencia.UnidadId" class="form-control" asp-items="@ViewBag.Unidad"></select>
            </div>
            <div class="form-group">
                <label asp-for="Persona_Dependencia.Dependencia" class="control-label">Dependencia</label>
                <select asp-for="Persona_Dependencia.DependenciaId" class="form-control" asp-items="@ViewBag.Dependencia"></select>
            </div>
            @*<div class="form-group">
            <label asp-for="Persona_Dependencia.Dependencia" class="control-label"></label>
            <input asp-for="Persona_Dependencia.DependenciaId" class="form-control" />
            <span asp-validation-for="Condicion" class="text-danger"></span>
        </div>*@
            @*<div class="form-group">
                <label asp-for="Roles" class="control-label"></label>
                <select asp-for="RolesId" class="form-control" asp-items="@ViewBag.Roles"></select>
            </div>*@
            <div class="form-group">
                @*@Html.DropDownListFor(x => x.Marca, (IEnumerable<SelectListItem>)ViewBag.Marca, "Porfavor seleccione una Marca", new { @class = "form-control" })*@
                <label asp-for="Persona_Dependencia.Persona.DNI" class="control-label"></label>
                <select asp-for="Persona_Dependencia.Persona.DNI" class="form-control" asp-items="@ViewBag.DNI"></select>
            </div>
            <div class="form-group">
                <label asp-for="Imei" class="control-label"></label>
                <select asp-for="Imei" class="form-control" asp-items="@ViewBag.Imei"></select>
            </div>
            @*<div class="form-group">
                <label asp-for="EstadoEquipo" class="control-label">Estado del Equipo</label>
                <select asp-for="EstadoEquipoId" class="form-control" asp-items="@ViewBag.Estado"></select>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Asignar equipo" class="btn btn-success ml-1" />
            </div>
        </form>
    </div>
</div>
