@model TelefoniaCargas.Models.Persona_Dependencia

@{ ViewData["Title"] = "Crear"; }

<div class="row">
    <div class="col-sm-12 p-3">
        <form method="post" asp-action="_Detalle">
            <input type="hidden" asp-for="Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Persona.Nombre" class="control-label"></label>
                <select asp-for="PersonaId" class="form-control" asp-items="@ViewBag.Nombre" disabled></select>
            </div>
            @*<div class="form-group">
                    <label asp-for="Persona.Apellido" class="control-label"></label>
                    <select asp-for="PersonaId" class="form-control" asp-items="@ViewBag.Apellido"></select>
                </div>*@
            @*<div class="form-group">
                    <label asp-for="Persona.DNI" class="control-label"></label>
                    <select asp-for="PersonaId" class="form-control" asp-items="@ViewBag.DNI"></select>
                </div>*@
            <div class="form-group">
                <label asp-for="Dependencia" class="control-label"></label>
                <select asp-for="DependenciaId" class="form-control" asp-items="@ViewBag.Dependencia" disabled></select>
            </div>
            <div class="form-group">
                @*<input type="submit" value="Crear Persona y Dependencia" class="btn btn-success ml-1" />*@
                <button class="btn btn-primary" type="button" data-dismiss="modal">Cerrar</button>
            </div>
        </form>
    </div>
</div>


